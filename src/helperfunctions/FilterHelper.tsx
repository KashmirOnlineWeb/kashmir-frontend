import format from "date-fns/format";
import { useRouter } from "next/router";
import { date } from "yup";

export const FilterHelpers = (key: string, value: any) => {
    // const router = useRouter();
    // router.query.key = value//format(date as unknown as Date, "dd-MM-yyyy");
    // router.push(router);
}